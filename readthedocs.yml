version: 2

build:
  os: ubuntu-22.04
  tools:
    python:
      version: "3.8"
      install:
        - method: pip
          packages:
            - torch==2.1.2
            - -r docs/requirements.txt

formats: []
