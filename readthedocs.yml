version: 2

build:
   os: ubuntu-22.04
   tools:
      python: "3.8"

python:
   install:
      - requirements: docs/requirements.txt
      - method: pip
        path: .

# 在安装主包之前手动安装 torch-sparse，确保顺序正确
build:
  os: ubuntu-22.04

conda: null

sphinx:
  builder: html

# 在 main install 之前安装 torch-sparse
pre_install:
  - pip install torch-sparse==0.6.12

formats: []
